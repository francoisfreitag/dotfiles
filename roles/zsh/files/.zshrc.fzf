# Fuzzy finder
source <(fzf --zsh)
export FZF_DEFAULT_OPTS='--tmux'
pytest_failed() {
    tmux capture-pane -S - -p | rg '^FAILED ' | cut -d' ' -f 2 | uniq | fzf --bind 'enter:become(pytest {})'
}
zle -N pytest_failed
bindkey '^v' pytest_failed
