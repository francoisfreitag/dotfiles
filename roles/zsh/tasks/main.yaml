---
- name: Install zsh
  pacman:
    name: zsh

- name: Install zsh configuration files
  file:
    src: "{{ role_path }}/files/{{ item }}"
    dest: "~/{{ item }}"
    state: link
  loop:
    - .zshrc.local
    - .zshrc.fzf
